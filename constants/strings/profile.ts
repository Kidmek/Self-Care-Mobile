export enum PROFILE_STRINGS {
  CHANGE_EMAIL = 'Change Email',
  CHANGE_PASSWORD = 'Change Password',
  PASSWORD = 'Current Password',
  PASSWORD_PLACEHOLDER = 'Enter your current password',
  CONFIRM = 'Confirm',
  WRONG_PASSWORD = 'Wrong Password',
  CHANGE = 'Change',
  NEW_EMAIL = 'New Email',
  NEW_EMAIL_PLACEHOLDER = 'Enter your new email',
}

// export const en_translations: { [placeholder in AUTH_STRINGS]: string } = makeObjects(
//   Object.values(AUTH_STRINGS)
// );

export const am_profile_translations: { [placeholder in PROFILE_STRINGS]?: string } = {};
